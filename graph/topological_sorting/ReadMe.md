# 拓扑排序
给出一个 有向图，把这个有向图转成线性的排序 就叫拓扑排序。
拓扑排序也是图论中判断有向无环图的常用方法

拓扑排序算法有两种，BFS  和  DFS
对于拓扑排序，一般用BFS比较多


拓扑排序的过程，其实就两步：
1. 找到入度为0 的节点，加入结果集
2. 将该节点从图中移除----指向的节点入度-1
3. 如果结果集的元素个数<图的节点个数，那么就说明存在有向环 （没有入度为0的节点了）

循环以上两步，直到 所有节点都在图中被移除了。
结果集的顺序，就是我们想要的拓扑排序顺序 （结果集里顺序可能不唯一）